<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sys.animation">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Add_new" />
        <activity android:name=".Update_new" />
        <activity android:name=".Myorder_details" />
        <activity android:name=".Review" />
        <activity android:name=".Bevarages"></activity>
        <activity android:name=".Delivery_address" />
        <activity android:name=".share_review" />
        <activity android:name=".Tabbed">

        </activity>
        <activity android:name=".Navigation_drawer">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>